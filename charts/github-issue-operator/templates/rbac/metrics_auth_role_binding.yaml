apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "github-issue-operator.name" . }}-metrics-auth-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "github-issue-operator.name" . }}-metrics-auth-role
subjects:
- kind: ServiceAccount
  name: {{ include "github-issue-operator.serviceAccountName" . }}
  namespace: {{ include "github-issue-operator.namespace" . }}
