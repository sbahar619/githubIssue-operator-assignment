---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: githubissues.github.shahaf.com
spec:
  group: github.shahaf.com
  names:
    kind: GithubIssue
    listKind: GithubIssueList
    plural: githubissues
    singular: githubissue
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GithubIssue is the Schema for the githubissues API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GithubIssue
            properties:
              description:
                description: Description is the GitHub issue body content
                maxLength: 65536
                type: string
              repo:
                description: Repo is the GitHub repository URL
                maxLength: 150
                minLength: 19
                pattern: ^https://github\.com/[a-zA-Z0-9_.-]+/[a-zA-Z0-9_.-]+$
                type: string
              title:
                description: Title is the GitHub issue title that will be created
                  or updated
                maxLength: 256
                minLength: 1
                type: string
            required:
            - repo
            - title
            type: object
          status:
            description: status defines the observed state of GithubIssue
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
